using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class stopp : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Physics.sleepAngularVelocity = 2f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
